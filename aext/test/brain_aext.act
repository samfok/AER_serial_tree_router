// test the aer transmitter specific to braindrop/brainstorm
import "aer/aext/aext.act";
import "aer/interface/interface.act";
pint M=3;
pint N=2;
pint NN=8; // NN=N^M
aer::globals g;
aer::active_handshaker<NN> a(g,,);
aer::BRAIN_AEXT<M, N, NN> aext(g, a.o, a.i,,,);
aer::interface::OUT_a1ofN<N> out(g, aext.pp, aext.pi, aext.p,);
aer::interface::DESERIAL_RING<M, N, false, true> deserial(g, out.y,);
aer::SINK_eMx1ofN<M, N> snk(g, deserial.y);
