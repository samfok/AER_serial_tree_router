This directory is for testing the interface with the brainstorm library.
These tests can only be run if this aer repository is positioned as a
directory within the brainstorm repository, namely as a directory within
brainstorm/act.
