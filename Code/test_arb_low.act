// Tests the ARB_LOW circuit
import "lib/globals.act";
import "arbiter.act";

globals g;
arbiter::test::TEST_ARB_LOW test(g);

bool pReset, sReset;
prs {
    sReset => g.sReset+
    pReset => g.pReset+
    sReset => g._sReset-
    pReset => g._pReset-
    sReset -> g.Vdd+
    sReset -> g.GND-
}
