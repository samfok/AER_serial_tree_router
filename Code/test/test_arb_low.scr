set_principal test.arb._ai[0]
set_principal test.arb._ao[0]
set_principal test.arb._ai[1]
set_principal test.arb._ao[1]

source reset.scr
random
break-on-warn

# watchall

# watch test.arb._ai[0]
# watch test.arb._ao[0]
# watch test.arb._ai[1]
# watch test.arb._ao[1]
# watch test.arb.a[0]
# watch test.arb.a[1]
