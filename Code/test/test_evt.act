import "lib/globals.act";
import "transmitter.act";

globals g;
transmitter::TEST_EVT test(g);

bool pReset, sReset;
prs {
    sReset => g.sReset+
    pReset => g.pReset+
    sReset => g._sReset-
    pReset => g._pReset-
    sReset -> g.Vdd+
    sReset -> g.GND-
}
