// TESTS the SIMPLE_AEXT circuit
import "lib/globals.act";
import "simple_aer.act";
import "MofN_table.act";

globals g;
aer::test::TEST_AEXT<M, N, NN> test(g);

bool pReset, sReset;
prs {
    sReset => g.sReset+
    pReset => g.pReset+
    sReset => g._sReset-
    pReset => g._pReset-
    sReset -> g.Vdd+
    sReset -> g.GND-
}

