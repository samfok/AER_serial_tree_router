// Tests the CTRL circuit
import "lib/globals.act";
import "arbiter.act";

globals g;
arbiter::ctrl_p::TEST_CTRL test(g);

bool pReset, sReset;
prs {
    sReset => g.sReset+
    pReset => g.pReset+
    sReset => g._sReset-
    pReset => g._pReset-
    sReset -> g.Vdd+
    sReset -> g.GND-
}
