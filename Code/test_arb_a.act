// Tests the arbiter cell with active S CTRL
import "lib/globals.act";
import "arbiter.act";

globals g;
arbiter::TEST_ARB_A test(g);

bool pReset, sReset;
prs {
    sReset => g.sReset+
    pReset => g.pReset+
    sReset => g._sReset-
    pReset => g._pReset-
    sReset -> g.Vdd+
    sReset -> g.GND-
}

