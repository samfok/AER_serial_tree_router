// Tests the C_S circuit for the CTRL with active S ports
import "lib/globals.act";
import "arbiter.act";

globals g;
arbiter::ctrl_p::TEST_C_S test(g);

bool pReset, sReset;
prs {
    sReset => g.sReset+
    pReset => g.pReset+
    sReset => g._sReset-
    pReset => g._pReset-
    sReset -> g.Vdd+
    sReset -> g.GND-
}

